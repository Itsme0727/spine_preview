# 轨道混合修复说明

## 本次核心整改

原版本的轨道序列在混合值大于 0 时，使用两条 Runtime 轨道分别播放前后动画，再手动修改两条轨道的 alpha 做淡入淡出。这种方式属于画面层面的伪交叉淡化，不是 Spine `AnimationState` 对同一轨道动画状态的真实姿势混合，因此会产生属性覆盖、轨道抢占、切换重置和多轨叠加异常。

本版已改为 Spine 原生逻辑：

1. 一条界面轨道直接对应一条 Spine Runtime 轨道，轨道编号不再使用 `T0→0/1、T1→2/3` 的双轨映射。
2. 同轨道第一个动画使用 `setAnimation`，后续动画使用 `addAnimation` 构建真实队列。
3. `A → B` 的混合值保存为 A 的 `mixOut`，并应用到 B 的 `TrackEntry.mixDuration`。
4. 同时修正 `TrackEntry.delay`，确保 0.5 秒混合从 A 结束前 0.5 秒开始，而不是等 A 播放完再额外淡化。
5. 混合期间由 Spine Runtime 自己计算 `mixingFrom / mixingTo`、骨骼、插槽、颜色、网格变形、约束等最终姿势，不再手动修改两层画面的透明度。
6. 多条轨道直接由同一个 `AnimationState` 从低轨到高轨应用到同一个 Skeleton：不控制相同属性时可同时播放；控制相同属性时由高轨道按 Replace/Alpha 规则影响下层。
7. 循环序列会预先排入下一轮，保证最后一个动画切回第一个动画时不会因队列来不及补充而中断。
8. 主画布和右上角动画预览浮窗共用同一套轨道队列构建逻辑，避免两处画面计算结果不一致。
9. 新增轨道默认使用“替换（Replace）”，更符合高轨覆盖同属性、保留未控制属性的常规 Spine 分层逻辑；仍保留“叠加（Add）”供特殊动画使用。
10. 轨道扫描上限统一为 10，和面板允许的最大轨道数一致。

## 重点修改文件

- `js/data-model.js`：原生队列构建、混合时间与 delay 修正、循环队列维护、多轨 Alpha 更新。
- `js/spine-rendering.js`：删除主画布和预览浮窗的双轨 alpha 状态机，改为维护 Spine 原生队列。
- `js/ui-dom.js`：轨道编号直连、启用/禁用逻辑、Alpha 同步、默认 Replace。
- `js/app.js`：轨道冻结/恢复扫描数量与 10 条轨道上限对齐。
- `index.html`：更新脚本版本参数，避免浏览器继续使用旧缓存。

## 建议验收

### 同轨道混合

- A 时长 2 秒，A→B 混合设置 0.5 秒。
- 预期：1.5 秒时 B 开始参与计算；2.0 秒时 B 完全接管。
- 不能出现两个完整人物画面重影，也不能等 A 播完后再额外花 0.5 秒切换。

### 零秒切换

- A→B 设置 0 秒。
- 预期：A 播放结束时直接进入 B，没有残留姿势、T Pose 或闪回初始帧。

### 多轨道

- T0：手部动画。
- T1：脸部动画。
- T2：脚部动画。
- 预期：三部分同时运动；没有控制同一属性时互不清除。

### 同属性覆盖

- 低轨道和高轨道同时控制脸部。
- 高轨道使用 Replace、Alpha=1 时，高轨道控制其有关键帧的脸部属性；未打关键帧的属性继续保留下层结果。

## 已完成检查

- 所有 JavaScript 文件通过 `node --check` 语法检查。
- 使用模拟 AnimationState 测试了：直接轨道编号、A→B 0.5 秒的 delay、B→C 混合、非循环队列、循环队列补充以及整条队列 Alpha 更新。

## 当前无法代替你完成的验证

你提供的压缩包内没有实际的 `.json/.skel + .atlas + .png` Spine 角色资源，因此本次无法在这里对具体角色做最终视觉对比。代码逻辑已经按 Spine 原生队列重构，仍需要你用实际资源验证画面；若某个特定资源仍异常，请保留该资源和明确的 A/B 动画名继续定位。

## 2026-07-23：新出现附件在混合期“从远处飞入”修复

现象：A 中光圈不可见，B 中光圈出现；A→B 设置 0.3/0.5 秒后，光圈会在过渡阶段从远处移动到目标位置。

原因：附件的世界位置不只由附件自身决定，还会继承父骨骼、全部祖先骨骼和约束。即使光圈 Slot 只做了显示/透明度动画，只要父级姿势仍在 A→B 混合，刚出现的光圈就会把父级位移过程暴露出来。此外，未被当前动画关键帧控制的骨骼属性可能保留上一帧姿势，造成残留。

本次处理：

1. 轨道模式每帧 `state.apply` 前，只恢复当前所有轨道与 `mixingFrom` 链完全未控制的骨骼到 Setup Pose；不重置正在动画中的骨骼、Slot、附件和透明度，因此不会破坏多轨叠加或物理约束。
2. 自动识别“混合开始前不可见、混合后新出现”的 Slot。
3. 仅当该 Slot 的父骨骼在混合期发生明显位移时，前 72% 暂时保持透明，最后 28% 平滑淡入。
4. 该 Alpha 处理只在渲染阶段临时生效，绘制后立即恢复，不修改 Spine 原始动画、关键帧或导出资源。
5. 主画布和右上角动画预览使用同一机制。

注意：如果光圈本来就应该跟随角色父骨骼移动，这个机制只会隐藏“新出现时的远距离飞入”，不会修改角色和骨骼的真实动画姿势。
